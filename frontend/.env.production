# =======================================
# Variáveis de ambiente para PRODUÇÃO
# =======================================
# Este arquivo é usado automaticamente pelo Vite durante o build de produção (npm run build)
#
# IMPORTANTE: Em produção, o frontend e backend rodam na mesma porta/domínio
# Por isso usamos um caminho relativo (/api) em vez de URL completa

# URL da API - Caminho relativo (mesma origem que o frontend)
VITE_API_URL=/api
